<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sign Up • Darkflix</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen font-sans text-white bg-black">
  <div class="fixed inset-0 bg-[url('https://images.pexels.com/photos/799137/pexels-photo-799137.jpeg')] bg-cover bg-center opacity-30"></div>
  <div class="fixed inset-0 bg-gradient-to-b from-black via-black/70 to-black"></div>

  <header class="relative z-10 max-w-5xl mx-auto px-4 py-4 flex items-center justify-between">
    <a href="/movies" class="flex items-center">
      <span class="text-3xl md:text-4xl font-black tracking-tight text-red-600">
        DARKFLIX
      </span>
    </a>
    <a href="/login"
       class="hidden sm:inline text-sm px-4 py-1.5 rounded bg-black/60 hover:bg-black/40 border border-white/20">
      Sign In
    </a>
  </header>

  <main class="relative z-10 flex items-center justify-center px-4 pt-10 pb-16">
    <div class="w-full max-w-md bg-black/80 rounded-md px-8 py-10 shadow-[0_0_40px_rgba(0,0,0,0.8)]">
      <h1 class="text-2xl md:text-3xl font-bold mb-6">Sign Up</h1>
      <% if (error) { %>
        <p class="mb-4 text-sm bg-[#e87c03] text-white rounded px-3 py-2">
          <%= error %>
        </p>
      <% } %>
      <form action="/signup" method="POST" class="space-y-4">
        <input type="text" name="name"
               value="<%= values && values.name ? values.name : '' %>"
               placeholder="Name"
               required
               class="w-full bg-[#333] text-sm rounded px-3.5 py-3 outline-none focus:ring-2 focus:ring-red-600 placeholder:text-slate-400" />
        <input type="email" name="email"
               value="<%= values && values.email ? values.email : '' %>"
               placeholder="Email"
               required
               class="w-full bg-[#333] text-sm rounded px-3.5 py-3 outline-none focus:ring-2 focus:ring-red-600 placeholder:text-slate-400" />
        <input type="password" name="password"
               placeholder="Password"
               minlength="6"
               required
               class="w-full bg-[#333] text-sm rounded px-3.5 py-3 outline-none focus:ring-2 focus:ring-red-600 placeholder:text-slate-400" />
        <input type="password" name="confirmPassword"
               placeholder="Confirm password"
               minlength="6"
               required
               class="w-full bg-[#333] text-sm rounded px-3.5 py-3 outline-none focus:ring-2 focus:ring-red-600 placeholder:text-slate-400" />
        <button type="submit"
                class="mt-2 w-full bg-red-600 hover:bg-red-700 text-sm font-semibold py-3 rounded">
          Create account
        </button>
      </form>
      <div class="mt-6 text-[13px] text-slate-300">
        <span class="text-slate-400">Already have an account?</span>
        <a href="/login" class="ml-1 text-white hover:underline">Sign in</a>
      </div>
    </div>
  </main>
</body>
</html>
