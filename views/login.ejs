<%-
  include("layout", {
    title: "Login • DarkAuth",
    body: `
      <section>
        <h2 class="text-lg font-semibold mb-2">Login</h2>
        <p class="text-xs text-slate-300 mb-4">
          Welcome back. Sign in to access your profile.
        </p>

        ${error ? `<p class="mb-3 text-xs text-red-400">${error}</p>` : ""}

        <form action="/login" method="POST" class="space-y-3">
          <div>
            <label class="block text-xs mb-1">Email</label>
            <input
              type="email"
              name="email"
              value="${values.email || ""}"
              required
              class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:outline-none"
            />
          </div>

          <div>
            <label class="block text-xs mb-1">Password</label>
            <input
              type="password"
              name="password"
              required
              class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:outline-none"
            />
          </div>

          <button
            type="submit"
            class="w-full mt-2 bg-red-600 hover:bg-red-500 text-xs font-semibold py-2 rounded">
            Login
          </button>
        </form>

        <p class="mt-4 text-xs text-slate-300">
          Don't have an account?
          <a href="/signup" class="text-red-400 hover:text-red-300">Sign up</a>
        </p>
      </section>
    `
  })
%>
