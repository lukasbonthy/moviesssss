<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Profile • DarkAuth</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background:
        radial-gradient(circle at 0% 0%, rgba(248, 113, 113, 0.18), transparent 60%),
        radial-gradient(circle at 100% 0%, rgba(56, 189, 248, 0.16), transparent 60%),
        #020617;
    }
  </style>
</head>
<body class="min-h-screen text-slate-100 antialiased">
  <div class="min-h-screen flex items-center justify-center px-4">
    <div class="w-full max-w-md bg-slate-950/80 border border-slate-800 rounded-2xl shadow-xl shadow-black/70 p-6">
      <%- include('partials/header') %>

      <section>
        <h2 class="text-lg font-semibold mb-2">Your profile</h2>
        <p class="text-xs text-slate-300 mb-4">
          Update your name and short bio.
        </p>

        <% if (message) { %>
          <p class="mb-3 text-xs text-emerald-400"><%= message %></p>
        <% } %>

        <% if (error) { %>
          <p class="mb-3 text-xs text-red-400"><%= error %></p>
        <% } %>

        <form action="/profile" method="POST" class="space-y-3">
          <div>
            <label class="block text-xs mb-1">Name</label>
            <input
              type="text"
              name="name"
              value="<%= user.name || '' %>"
              required
              class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:outline-none"
            />
          </div>

          <div>
            <label class="block text-xs mb-1">Email</label>
            <input
              type="email"
              value="<%= user.email %>"
              disabled
              class="w-full bg-slate-900 border border-slate-800 rounded px-3 py-2 text-sm text-slate-400"
            />
          </div>

          <div>
            <label class="block text-xs mb-1">Bio</label>
            <textarea
              name="bio"
              rows="3"
              class="w-full bg-slate-900 border border-slate-700 rounded px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:outline-none resize-none"
            ><%= user.bio || '' %></textarea>
          </div>

          <button
            type="submit"
            class="w-full mt-2 bg-red-600 hover:bg-red-500 text-xs font-semibold py-2 rounded">
            Save changes
          </button>
        </form>

        <p class="mt-4 text-[11px] text-slate-400">
          Account created at: <%= new Date(user.created_at).toLocaleString() %>
        </p>
      </section>
    </div>
  </div>
</body>
</html>
